@model List<Models.EF.Menu>
<div class="call">
    <p><span>cần hỗ trợ ?</span>gọi ngay <span class="number">123456789</span></span></p>
</div>
<div class="account_desc">
    @foreach (var item in Model)
    {
        <li>
            <a href="@item.Link" target="@item.Target">@item.Text</a>

        </li>
    }
</div>
<div class="clear"></div>
            
